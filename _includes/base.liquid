<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Evan's Demos</title>
    <link rel="stylesheet" href="demo/main.css" />
    <link rel="stylesheet" href="demo{{ page.url }}index.css" />
  </head>
  <body>
    {% if page.url != "/"%}
      <a class="back" href="/">Back</a>
    {% endif %}
    <main class="mb-6 leading-relaxed" id="main">{{ content }}</main>
  </body>
</html>
