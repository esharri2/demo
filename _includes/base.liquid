<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Evan's Demos</title>
    <link rel="stylesheet" href="{{ '/main.css' | htmlBaseUrl }}" />
    <link rel="stylesheet" href="{{ page.url | append: "index.css" | htmlBaseUrl }}" />
    <script type="module" src="{{ page.url | append: "index.js" | htmlBaseUrl }}"></script>

  </head>
  <body>
    {% if page.url != "/"%}
      <a class="back" href="/">Back</a>
    {% endif %}
    <main id="main">{{ content }}</main>
  </body>
</html>
