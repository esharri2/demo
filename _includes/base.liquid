<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Evan's Demos</title>
    <link rel="stylesheet" href="{{ '/main.css' | htmlBaseUrl }}" />
    <link rel="stylesheet" href="{{ page.url | append: "index.css" }}" />
    <script type="module" src="{{ page.url | append: "index.js"  }}"></script>

  </head>
  <body>
    {% if page.url != "/"%}
      <a class="back" href="/">Back</a>
    {% endif %}
    <main id="main">{{ content }}</main>
  </body>
</html>
